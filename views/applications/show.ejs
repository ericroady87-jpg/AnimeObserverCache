<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/stylesheets/show.css" />
  <title>
    <%= application.title %>
  </title>
</head>

<body>
  <%- include('../partials/_navbar.ejs') %>
    <div class="show-body">
      <div class="show-header">
        <h1>Title: <%= application.showTitle %> Genre:<%= application.genre %>
        </h1>
        <div class="show-sidebar">
          <div class="show-actions">
            <a href="/users/<%= user._id %>/applications/<%= application._id %>/edit"> Edit Title</a>
            <form action="/users/<%= user._id %>/applications/<%= application._id %>?_method=DELETE" method="POST">
              <button type="submit">Delete Show</button>
            </form>
          </div>
          <p>Current Status: <%= application.status %>
          </p>
        </div>
      </div>
      <div class="show-notes-container">
        <% if (application.whereToWatch) { %>
          <a href="<%= application.whereToWatch %>" target="_blank">View Show Status</a>
          <% } %>
            <% if (application.comments) { %>
              <div class="show-notes">
                <h2>Comments:</h2>
                <p>
                  <%= application.comments %>
                </p>
              </div>
              <% } %>
      </div>
    </div>
</body>

</html>